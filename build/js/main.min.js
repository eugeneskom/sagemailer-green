"use strict";

var toggle = document.querySelector(".nav__toggle"),
    menuBox = document.querySelector(".nav__links"),
    menuLine = document.querySelector(".nav__line");

function menuToggle() {
  menuBox.classList.toggle("nav__links--active"), menuLine.classList.toggle("nav__line--active");
}

toggle.addEventListener("click", menuToggle);